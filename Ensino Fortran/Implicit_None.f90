program name
    
    ! Sem o Implicit None, muitas coisas não precisam declarar seu tipo
    ! inteiros --> i, j, k, l, m, n !Começando com essas variaveis, serão inteiros como, joao, ksksks
    ! reais --> a, b, t, x, y

    ! implicit character (a,b,c)
    ! implicit character(10) (a-z)

    implicit integer(i-n), real(d-h, o-z), character(10) (a,b,c)
    print*, i, d, a

end program name